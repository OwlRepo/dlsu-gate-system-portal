# QA Report

Required sections — the five buckets from `agents/qa.md`. A bucket with no scenarios listed counts as incomplete coverage, not a pass.

Happy Path:

Error Cases:

Edge Cases:

Rare / Boundary Edge Cases:

Optimization / Performance-Relevant Cases:

Results:

Failures:

Evidence:

FE+BE gate (if applicable): gstack `/qa` or `/qa-only` run — Yes/No, result.
