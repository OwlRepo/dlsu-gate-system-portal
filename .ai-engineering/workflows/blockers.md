# Blocker Handling

Stop and report when:

- requirements are unclear
- dependency is unavailable
- security impact exists
- business decision is needed
- architecture is uncertain
- a task hits `UNVERIFIED DEPENDENCY` (see `workflows/task-intake.md`) and cannot be resolved from source alone
