# Project Memory

This file is a pointer, not a store. Durable project facts — architecture, business rules, constraints, contracts — live in `knowledge/` (`architecture.md`, `api-contracts.md`, `db-contracts.md`, `module-ownership-map.md`, `risk-register.md`, `testing-strategy.md`, `environment.md`, `repository-map.md`). Use `workflows/context-refresh.md` to update those.

This file records session-discovered items that haven't been promoted into `knowledge/` yet — a scratch layer, not a second copy of the facts above. When something here stabilizes into a durable fact, move it into the matching `knowledge/*` file and remove it here.

Session notes (append below, promote when stable):
