# Project Memory

This file is a pointer, not a store. Durable project facts — architecture, business rules, constraints, contracts — live in the `docs/ai/` maps (`docs/ai/architecture-manifest.md`, `docs/ai/contracts/api-contracts.md`, `docs/ai/contracts/db-contracts.md`, `docs/ai/module-ownership-map.md`, `docs/ai/risk-register.md`, `docs/ai/testing-strategy.md`, `docs/ai/dev-environment.md`, `docs/ai/file-index/repository-map.md`). Use `docs/ai/context-refresh.md` to update those.

This file records session-discovered items that haven't been promoted into `docs/ai/` yet — a scratch layer, not a second copy of the facts above. When something here stabilizes into a durable fact, move it into the matching `docs/ai/*` file and remove it here.

Session notes (append below, promote when stable):
