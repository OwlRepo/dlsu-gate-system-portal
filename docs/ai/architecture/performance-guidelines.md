# Performance Guidelines
- Frontend: avoid unnecessary rerenders, heavy client components, and request waterfalls.
- Backend: avoid N+1 queries, paginate list endpoints, validate payloads, and avoid blocking ops.
