# Security Guidelines
- Validate all input DTOs/server payloads.
- Enforce auth and role guards.
- Keep secrets in root `.env` and never print values.
- Preserve API contract compatibility unless approved.
