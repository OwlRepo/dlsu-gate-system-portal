# Testing Strategy
- Backend unit/integration: Jest in `apps/backend/src/**/*.spec.ts`
- Backend e2e: `apps/backend/test/*`
- Frontend: no dedicated test suite detected in current import.
Minimum verification: lint + typecheck + targeted backend tests + builds.
