# Risk Register

Purpose:

Map high-risk project areas.

This file is map only.

It is not proof of behavior.

Mark missing risk areas as `UNMAPPED RISK`.

## Rules

- If task touches listed high-risk area, default to Deep
- Only downgrade Deep if repository evidence proves task is isolated and low-risk
- Verify risk against source code and related contracts
- If risk area is missing, mark `UNMAPPED RISK`

## Risk Areas

| Risk Area | Why Risky | Default Task Size | Required Checks | Manual QA | Notes |
| --- | --- | --- | --- | --- | --- |
| Auth & Sessions | `apps/backend/src/auth/jwt-auth.guard.ts` has a DEV-MODE BYPASS that fabricates `request.user` as SUPER_ADMIN when `NODE_ENV=development` and no token is present. `apps/backend/src/login/login.service.ts:108` signs admin JWTs with `role:'ADMIN'` (uppercase) while `Role.ADMIN` = `'admin'` (lowercase) in `role.enum.ts`, silently breaking `@Roles(Role.ADMIN)` checks for admin-issued tokens. Zero unit tests exist for `src/auth/`. | Deep | authentication tests, authorization boundary tests, explicit test of the dev-mode bypass path, token-blacklist/session tests | full login/logout flow across admin, super-admin, and employee login paths | Global `JwtAuthGuard` protects every route unless `@Public()` — changes here have system-wide blast radius. Dead files in this area: `src/auth/guards/jwt-auth.guard.ts` (stub) and `src/auth/jwt.strategy.ts` (duplicate) — do not edit these, edit the real files (`src/auth/jwt-auth.guard.ts`, `src/auth/strategies/jwt.strategy.ts`). |
| Database Sync / BioStar Integration | `apps/backend/src/database-sync/` pulls student/entrant PII and photos from an external SQL Server, writes to Postgres `students`, and pushes to BioStar physical access-control hardware via `services/shared/biostar-api.service.ts`. `POST /database-sync/sync` and `POST /database-sync/biostar/sync` have NO role guard (JWT only). Zero unit tests exist for `src/database-sync/`. Credentials for both external systems are in play. | Deep | sync idempotency tests, `studentMutationLock` concurrency tests, external-service failure/rollback tests, role-guard coverage for trigger endpoints | full manual-sync and scheduled-sync flow against a non-production SQL Server/BioStar instance if available | Two source-schema strategies selected by `SOURCE_DB_SCHEMA_ENV` (`main` vs `dasma`) — verify which applies before changing sync logic. Writes plaintext audit logs to `logs/skipped-records`, `logs/synced-records`, `logs/photo-conversion`. Global `studentMutationLock` mutex is the only concurrency guard on the `students` table. |
| Reports / Gate Access Log | `reports` table (`src/reports/entities/report.entity.ts`) is the authoritative gate access-decision log (`status` field drives allow/deny history). `ReportsGateway` (`src/reports/reports.gateway.ts`) broadcasts live stats over Socket.IO at `/socket.io/` (`stats-update` event) with NO AUTH GUARD at all. | Deep | write-path tests for POST `/reports` (single + bulk), websocket auth-gap awareness, campus-aware status-mapping tests (`lib/access-status.ts`) if touched | verify entry/exit events land correctly for both DASMA (3-tier GREEN/YELLOW/RED) and MTL (binary allowed/not-allowed) campus modes | `ReportsGateway` polls every 1s via `@Interval` — changes to polling cadence or payload shape affect all connected dashboard clients including unauthenticated ones. |
| Admin / Super-Admin / User Account Management | `AdminController` (`src/admin/admin.controller.ts`) has NO role guard at all — any authenticated user can read/patch admin records. `POST /super-admin/register` (`src/super-admin/super-admin.controller.ts`) has NO role check — any authenticated user can create a super-admin account, a direct privilege-escalation path. Several super-admin routes use manual in-controller role checks instead of `RolesGuard`. Zero unit tests for `src/super-admin/` and `src/users/`. | Deep | authorization boundary tests for every route on `AdminController` and `SuperAdminController`, explicit privilege-escalation regression test for `/super-admin/register` | attempt each affected endpoint as a lower-privileged authenticated user and confirm current (broken) behavior before and after any fix | Any task that adds a role guard here is itself a Deep/security-sensitive change — coordinate before changing default access behavior. |
| Database Migrations | `apps/backend/src/main.ts` runs `AppDataSource` migrations automatically on every boot — a bad migration blocks application startup entirely. Three inconsistent DataSource configs exist: `src/config/data-source.ts` (canonical, used by `migration:*` scripts and `main.ts`), `src/data-source.ts` (root, unused duplicate), `src/config/typeorm.config.ts` (used by `typeorm:*` scripts), `src/config/database.config.ts` (dead/unused). A duplicate migration file (`AddActivationColumnsToUsers`) exists twice in `src/migrations/`. | Deep | migration up/down tests, verify against the canonical `src/config/data-source.ts`, confirm which config a given npm script actually uses before assuming consistency | run migration against a disposable/staging database and confirm boot succeeds before merging | Because migrations auto-run on boot, a broken migration is a production-down incident, not just a failed deploy step. |
| External Integrations (SQL Server + BioStar) | `mssql` package connects to an external SQL Server as the source-of-truth for student/entrant rosters; `biostar-api.service.ts` talks to Suprema BioStar 2 physical access-control hardware. Both are outside this repo's control — schema drift, credential rotation, or hardware firmware changes can silently break sync. | Standard (Deep if change touches sync triggers, credentials, or schema mapping) | mock/stub external responses in tests, verify schema-mapping assumptions (`main` vs `dasma`) against current source schema before changing field mappings | manual test-connection check via `GET /database-sync/test-connection` before and after change | Coordinate with whoever owns the external SQL Server/BioStar deployment before schema-mapping changes. |
