# Module Ownership Map

Purpose:

Map product/business domains to implementation areas.

This file is map only.

It is not proof of behavior.

Verify all conclusions against real source code, tests, types, schemas, routes, controllers, services, stores, components, API contracts, and database definitions.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTEXT DRIFT`.

Mark missing domains as `UNMAPPED DOMAIN`.

## Domain Index

| Domain | Frontend | Backend | Database / Schema | Jobs / Automations | Integrations | Tests | Risk | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Auth / Sessions | `lib/auth-context.tsx` (`useAuth().login()`), `lib/axios-interceptor.ts` (401 response interceptor), `src/middleware.ts` (route protection) | `src/auth/` — `jwt.strategy.ts` (real), `jwt-auth.guard.ts` (real, GLOBAL guard, has dev-mode bypass), `token-blacklist.service.ts`, `guards/roles.guard.ts`, `decorators/roles.decorator.ts`, `enums/role.enum.ts` | `token_blacklist` table (`src/auth/entities/token-blacklist.entity.ts`) | None | None | Zero unit tests for `src/auth/` | Deep | Dev-mode bypass fabricates `request.user` as SUPER_ADMIN when `NODE_ENV=development` and no token is present. Token expiry 2d, no refresh endpoint. Single-active-token-per-(userId, role) model via token blacklist. |
| Login | `components/custom/LoginForm.tsx`, `/login`, `/login/admin`, `/login/employee` routes | `src/login/` — `login.controller.ts`, `login.service.ts` | Reads `admin`, `super-admin`, `employee` tables | None | None | `src/login/*.spec.ts` exists | Deep | KNOWN BUG: `src/login/login.service.ts:108` signs admin JWTs with `role:'ADMIN'` (uppercase) but `Role.ADMIN` enum = `'admin'` (lowercase) — breaks `@Roles(Role.ADMIN)` checks for admin-login tokens. `src/login/entities/login.entity.ts` is a dead empty stub (not a real `@Entity`). |
| Admin Accounts | `/user-management` route, `components/users/AdminForm.tsx` | `src/admin/` — `admin.controller.ts`, `admin.service.ts`, `entities/admin.entity.ts` | `admin` table | None | None | `src/admin/*.spec.ts` exists | Deep | `AdminController` has NO role guard at all (only global JWT) — any authenticated user can call GET/PATCH `/admin/*`. |
| Super Admin Accounts | `/user-management` route, `components/users/SuperAdminForm.tsx` | `src/super-admin/` — `super-admin.controller.ts`, `super-admin.service.ts`, `entities/super-admin.entity.ts` | `super-admin` table | None | None | Zero unit tests for `src/super-admin/` | Deep | `POST /super-admin/register` has NO role check — any authenticated user can create a super-admin. `POST /super-admin/create-admin` and `GET /super-admin/:super_admin_id` do manual in-controller role checks instead of `RolesGuard`. |
| User Directory (cross-entity) | `/user-management` route (client-only, `ssr:false`), `components/users/` (`UserManagementPageContainer`, `EditDetailsDialog`, `ViewProfileDialog`, own duplicated `CustomDropdown`/`CustomTable`) | `src/users/users.controller.ts`, `src/users/users.service.ts` | Reads across `admin`, `super-admin`, `employee` tables | None | None | Zero unit tests for `src/users/` | Deep | Bulk deactivate/reactivate endpoints use `@Roles(SUPER_ADMIN, ADMIN)` — affected by the login.service.ts:108 role-casing bug for admin-issued tokens. |
| Employee Accounts | `/user-management` route, `components/users/EmployeeForm.tsx`, `/employee-dashboard` (TurnstileDashboard) | `src/employee/` — `employee.controller.ts`, `employee.service.ts`, `entities/employee.entity.ts` | `employee` table (uuid id, `device_id` json array) | None | None | `src/employee/*.spec.ts` exists | Standard | POST/PATCH gated by `RolesGuard` `@Roles(ADMIN, SUPER_ADMIN)`; GET routes only need global JWT. |
| Students Roster | No direct FE surface — consumed by gate hardware/mobile clients via Sync domain | `src/students/students.controller.ts`, `src/students/students.service.ts`, `entities/student.entity.ts` | `students` table (mirrors external SQL Server source; `isArchived`, `group` enum EMPLOYEE/STUDENT/AGENCY) | Populated by Database Sync domain | External SQL Server (indirect, via Database Sync) | Zero unit tests for `src/students/` | Standard | Mutations serialized via global `studentMutationLock` async mutex owned by the Database Sync module. |
| Reports (Gate Events) | `/reports` route, `components/reports/` (`ReportsPageContainer`, `ReportsTable`, `GateUsageChart`), `src/hooks/useReportSocket.tsx` | `src/reports/reports.controller.ts`, `src/reports/reports.service.ts`, `src/reports/reports.gateway.ts` (Socket.IO), `entities/report.entity.ts` | `reports` table (authoritative gate access-decision log; `status` e.g. `GREEN;allowed` / `RED;...` / `YELLOW;pending`; `gate` column added by `AddGateColumnToReports` migration) | `ReportsGateway` polls every 1s via `@Interval` and emits `stats-update` | BioStar device scan feed (indirect, via dashboard raw WebSocket to `/wsapi`) | `src/reports/*.spec.ts` exists (backend); `components/reports/*.test.tsx` (frontend, campus-mode focused only) | Deep | `ReportsGateway` websocket has NO AUTH GUARD at all. |
| Sync (mobile/offline pull) | No direct FE surface — consumed by gate hardware/mobile clients | `src/sync/sync.controller.ts`, `src/sync/sync.service.ts` | Reads `students` (non-archived) and `employee` (active only, password excluded) tables | None | None | Zero unit tests for `src/sync/` | Standard | Read-only pull endpoints for offline/mobile clients. |
| Database Sync (external SQL Server + BioStar integration) | No direct FE surface — triggered via `/settings` admin actions or scheduled jobs, consumed indirectly by gate hardware | `apps/backend/src/database-sync/` — `database-sync.controller.ts`, `database-sync.service.ts`, `database-sync-queue.service.ts`, `services/shared/biostar-api.service.ts`, `entities/sync-schedule.entity.ts`, `entities/sync-queue.entity.ts`, `entities/biostar-sync-state.entity.ts` | `students` table (writes), `sync_schedule`, `sync_queue`, `biostar_sync_state` tables | `cron` package + Nest `SchedulerRegistry`, timezone hardcoded `Asia/Manila`, default schedules 09:00/21:00; global `studentMutationLock` mutex serializes all sync ops | External SQL Server (`mssql` package, schema selected by `SOURCE_DB_SCHEMA_ENV` = `main`/`dasma`), BioStar 2 physical access-control hardware API | Zero unit tests for `src/database-sync/` | Deep | Highest-risk module: bulk PII/photo sync, physical hardware credentials. `POST /database-sync/sync` and `POST /database-sync/biostar/sync` have NO role guard (JWT only). Writes plaintext audit logs to `logs/skipped-records`, `logs/synced-records`, `logs/photo-conversion`. `@CacheTTL()` decorator on some routes is a no-op (never read by interceptor). |
| Screensaver | `/settings/operation` route, `components/settings/screen-saver-upload.tsx` | `src/screensaver/screensaver.controller.ts`, `src/screensaver/screensaver.service.ts` | No dedicated table confirmed — `TODO: Fill after repository analysis. Do not treat as verified.` | None | Static file serving via `persistent_uploads/` | Zero unit tests for `src/screensaver/` | Standard | Upload endpoint (multipart, 10MB limit) uses a manual in-service super-admin check, NOT `RolesGuard`. |
| Health | No direct FE surface | `src/health/health.controller.ts` | None | None | None | Zero unit tests for `src/health/` | Express | `GET /health` inherits the GLOBAL `JwtAuthGuard` with no `@Public()` — unusual for a health-check endpoint; would 401 an unauthenticated load-balancer probe. |
